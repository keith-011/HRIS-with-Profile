import MoreVertIcon from "@mui/icons-material/MoreVert";

const ActionTableData: React.FC = () => {
  return (
    <>
      <td className="table-data flex items-center justify-center">
        <button>
          <MoreVertIcon />
        </button>
      </td>
    </>
  );
};

export default ActionTableData;
